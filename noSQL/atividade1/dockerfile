# 1. Usa uma imagem oficial do Python como base
FROM python:3.9-slim

# 2. Define o diretório de trabalho dentro do container
WORKDIR /app

# 3. Copia o arquivo de dependências para o container
COPY requirements.txt .

# 4. Instala as dependências
RUN pip install --no-cache-dir -r requirements.txt

# 5. Copia o restante do código para o container
COPY . .

# 6. Informa qual porta a aplicação vai usar
EXPOSE 5000

# 7. Comando para rodar a aplicação
CMD ["python", "app.py"]